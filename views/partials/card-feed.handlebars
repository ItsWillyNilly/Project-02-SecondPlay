
    <a href="/card/{{id}}">
{{!-- we have to modify the container to in css file --}}
        <div class="container">
            <div class="header">
                <h2>{{title}}</h2>
            </div>
            <div class="body">
                <img src={{image_url}} alt="" width="80px" height="80px"/>
                <p>{{price}}</p>
                <p>{{condition}}</p>
                <p>{{description}}</p>
            </div>
        </div>
    </a>
